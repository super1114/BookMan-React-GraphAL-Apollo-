import React, { ReactElement } from 'react'

interface Props {
  
}

export default function BookInfo({}: Props): ReactElement {
  return (
    <div className="books_info_container">
      
    </div>
  )
}
